#!/bin/bash
if [ `ps -C haproxy --no-header | wc -l`  =  0 ]; then
        systemctl start haproxy
        sleep 3
        if [ `ps -C haproxy --no-header | wc -l`  =  0 ]; then
                systemctl stop keepalived
        fi
fi